module Lets.Go where

{-

Prepare your environment:

In the checkout folder, do the following:

$ cabal sandbox init ; cabal install --only-dependencies --enable-tests

You should see something like the following:




-}
